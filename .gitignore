# --- 1. 安全性 (最重要！絕對不能上傳) ---
.env                # 你的資料庫密碼都在這裡，上傳就資安外洩了！
.venv/              # 虛擬環境 (如果有的話)
venv/

# --- 2. 資料庫數據 (絕對不能上傳) ---
mysql_data/         # 這是幾百 MB 的資料庫本體，上傳會塞爆 GitHub，且沒意義

# --- 3. 垃圾檔案 (Python 編譯檔) ---
__pycache__/        # 這是 Python 執行時產生的快取，換台電腦就沒用了
*.pyc
*.pyo
*.pyd

# --- 4. 系統垃圾 ---
.DS_Store           # Mac 的縮圖設定檔
Thumbs.db           # Windows 的縮圖設定檔

# --- 5. 執行紀錄 ---
logs/               # 你的 Log 檔案，通常不需要上傳 (除非你想留底)
*.log

# --- 6. IDE 設定 (VS Code / PyCharm) ---
# 這些是你個人的編輯器設定，不用強迫同事跟你一樣
.vscode/
.idea/